apiVersion: networking.k8s.io/v1
kind: Ingress

metadata:
  name: bank-ingress

spec:
  ingressClassName: nginx

  rules:
  - host: node1

    http:

      paths:

      - path: /
        pathType: Prefix

        backend:
          service:
            name: frontend-service
            port:
              number: 80
